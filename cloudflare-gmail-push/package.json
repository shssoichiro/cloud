{
  "name": "cloudflare-gmail-push",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsgo --noEmit --incremental false",
    "lint": "pnpm run lint:oxlint",
    "lint:oxlint": "pnpm -w exec oxlint --config .oxlintrc.json cloudflare-gmail-push/src",
    "cf-typegen": "wrangler types --env-file /dev/null"
  },
  "dependencies": {
    "hono": "catalog:",
    "jose": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:",
    "vitest": "~3.2.4",
    "wrangler": "catalog:"
  }
}
