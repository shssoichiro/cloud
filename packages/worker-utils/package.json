{
  "name": "@kilocode/worker-utils",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsgo --noEmit --incremental false",
    "lint": "pnpm run lint:oxlint",
    "lint:oxlint": "pnpm -w exec oxlint --config .oxlintrc.json packages/worker-utils/src"
  },
  "dependencies": {
    "aws4fetch": "catalog:",
    "hono": "catalog:",
    "jose": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:",
    "vitest": "~3.2.4"
  }
}
